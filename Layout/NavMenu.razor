<nav class="main-nav">
    <div class="nav-container">
        <a class="brand" href="">Blazor Showcase</a>
        <button title="Navigation menu" class="nav-toggle" @onclick="ToggleNavMenu">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </button>
        <div class="@NavMenuCssClass nav-menu">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                Home
            </NavLink>
            <div class="dropdown">
                <span class="nav-link dropdown-toggle">Showcases</span>
                <div class="dropdown-menu">
                    <NavLink class="dropdown-item" href="minions-avanzamos">
                        Minions × Avanzamos
                    </NavLink>
                </div>
            </div>
        </div>
    </div>
</nav>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapsed" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
